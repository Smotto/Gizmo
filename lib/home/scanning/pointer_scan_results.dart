import 'package:flutter/material.dart';

class PointerScanResults extends StatelessWidget {
  const PointerScanResults({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
